# 更新日志

## 0.11.2 - 2026-08-31

### 修复

- 文件绑定到独立文件 Tab 后不再被下一个文件树预览替换，切换文件 Tab 会正常激活对应文件。
- 右侧面板和文件区的拖拽高亮只显示在当前分隔条上，拖拽宽度继续直接跟随指针。

## 0.11.1 - 2026-08-30

### 兼容性

- 兼容 DeepSeek Harness 控制器工作区投影（`byId` + `order`），同时保留旧版工作区列表格式。
- 文件引用同时支持拆分后的控制器会话输入接口和旧版 composer 接口。
- 默认安装路径继续使用稳定版 client runtime，同时在运行时兼容新版控制器数据结构。

## 0.11.0 - 2026-08-26

### 新功能

- 将 diff 审阅重构为 Codex 风格的审阅 Tab，在一个面板中按文件展示所有变更。
- 新增独立的审阅 Tab 和文件 Tab，支持空文件 Tab 隔离及 session 状态恢复。
- 支持 diff 文件折叠、复制文件路径、文件树 diff 导航和精确定位到目标 diff。
- 图标与 DSH primitives 对齐，对未提供的图标使用安全的本地实现。

### 修复

- 修复切换 Tab 或 session 时文件 Tab 与审阅 Tab 状态互相污染。
- 修复编辑链接定位到审阅面板、读取链接打开文件 Tab 时的复用和选中问题。
- 修复 agent 产生新变更时文件树和审阅状态未保持的问题。

## 0.10.0 - 2026-08-24

### 新功能

- 将 diff 审阅布局对齐 Codex 风格，并跟随 session 状态切换。
- diff 模式下只展示有变更的文件，并用图标区分新增、删除和混合变更。

### 修复

- 切换 session 时重置旧 Tab 和 diff 状态，并为有变更的 session 恢复 diff 模式。
- 修复关闭最后一个 diff Tab 后被自动重新打开的问题。

## 0.9.0 - 2026-08-20

### 新功能

- 新增 DSH 原生文件树右键操作：打开或定位文件、引用到对话、复制绝对路径。
- 对话中的文件引用改为原生输入框标签。

### 修复

- 修复 DSH 原生 tooltip 未锚定在工作台控件上的问题。

### 变更

- 客户端 bundle 改为通过 DSH module-loader factory 加载，并复用宿主 Tooltip 组件。

## 0.8.0 - 2026-08-19

### 新功能

- 新增工作区内容搜索，快捷键为 `⌘/Ctrl+⇧+F`。
- 显示命中行，并可直接打开文件定位到命中行。

## 0.7.0 - 2026-08-17

### 新功能

- 在变更审阅栏显示捕获 DSH 写入的简单操作摘要。
- 跟随 agent 写入，自动打开最新写入文件并在工作区文件树中定位。

## 0.6.0 - 2026-08-16

### 新功能

- 为工作台的纯图标控件新增聚焦、可键盘访问的 tooltip。
- 新增文件栏显隐记忆，并在打开或关闭 Quick Open 时恢复焦点。

### 变更

- 收敛工作台交互 token 和共用按钮状态，使悬停、按下、选中和焦点样式与 DSH 主界面保持一致。
- 简化文件树和审阅栏的切换，同时让隐藏控件退出键盘导航。
- 工作台样式改为在文档级只注入一次，不再随每个 UI 入口重复渲染。

### 修复

- 移除 Quick Open 搜索框中重复出现的浏览器原生清除按钮。
- 指针离开控件后，不再触发残留的 tooltip 延迟显示。

## 0.5.0 - 2026-08-16

### 新功能

- 新增变更审阅面板，按会话展示真实 DSH 写入的文件和 `+/−` 行数。

### 变更

- 审阅栏与文件树对齐：同一顶栏高度、行高和悬停样式。
- 顶栏审阅摘要并进 Workbench 按钮，有写入时在同一控件上显示 `+/−`。
- 去掉审阅栏刷新按钮，列表已跟随会话和磁盘事件。
- 去掉底栏、顶栏文件刷新、标签 Diff 标记和复制内容。
- 去掉审阅筛选和文件树的一键展开 / 刷新。树仍随磁盘变更重扫，文件夹仍点行展开。
- 当前会话有写入时，从 Workbench 按钮进入会落到审阅栏。

### 修复

- 跟随 DSH 的 Workspace 选择，文件树、标签页和审阅列表会切到新目录，而不再停在进程启动目录。
- 审阅 `+/−` 与展开后的预览 diff 一致；Workbench 按钮等当前会话就绪后再显示写入。

## 0.4.0 - 2026-08-16

### 新功能

- 新增工作区图片和渲染后的 Markdown 预览，支持 Markdown 中的相对图片。
- 新增 Markdown 预览 / 原始内容模式切换，原始内容继续使用 CodeMirror 查看。
- 使用 `marked` 渲染 GFM，并使用 DOMPurify 清洗渲染后的 HTML。

### 修复

- 图片通过独立资源预览接口支持最大 12 MB，同时保留普通文本预览限制。

## 0.3.2 - 2026-08-15

### 修复

- 只在客户端订阅磁盘变更事件时才启动文件监听，避免 Linux CI 上 `pnpm test` 退不出去。

## 0.3.1 - 2026-08-15

### 新功能

- 把工作台开关放到会话顶栏，紧挨 Session log。

### 修复

- 启动工作区文件监听时跳过 `node_modules` 等依赖目录，避免 Linux CI 在 `pnpm test` 里挂死。

## 0.3.0 - 2026-08-15

### 新功能

- 新增工作区文件树：面包屑、显隐、键盘导航、一键折叠，以及只定位不打开的树搜索。
- 新增 Quick Open（`⌘/Ctrl+P`）和 VS Code 式斜体预览标签；双击即可固定。对话里的写入仍会打开固定标签。
- 新增文件内查找 / 跳行、对话里的 `:line` / `#Lline` 定位，以及拖拽或右键把路径插入输入框。
- 新增语法高亮、代码折叠，以及磁盘变更后的实时刷新。

### 文档

- 更新演示图，并把 roadmap 对准 Codex 级文件工作区体验。

## 0.2.4 - 2026-08-14

### 修复

- 加强文件工作区交互流程，覆盖面板开关、调整宽度、标签、刷新、复制、加载/错误状态和 read/diff 展示。
- 为工作区控件和标签补充无障碍标签与状态属性。

### 测试

- 新增真实 read/edit 工具行和右侧工作区的组件级回归测试。

## 0.2.3 - 2026-08-14

### 文档

- 按标准 DSH 插件流程整理发布版安装和本地开发文档。

## 0.2.2 - 2026-08-14

### 修复

- 将 README 中的 DSH Web 启动命令同步为当前 CLI 语法。
- 一键启动在 Unix 下不再依赖 shell 执行。

## 0.2.1 - 2026-08-14

### 修复

- 一键启动默认改用 `pnpm dlx`，与 README 和仓库包管理约定保持一致。

## 0.2.0 - 2026-08-14

### 新功能

- 新增常驻、可调整宽度的右侧文件工作区和多文件标签。
- 新增读取只读预览，以及写入/编辑的真实 CodeMirror Diff。
- 新增刷新、复制内容、复制路径、宽度记忆和桌面快捷键。
- 新增安全的 README 实机演示图并更新项目元信息。

### 修复

- 修复文件写入后读取预览被误判成 Diff 的问题。
- 支持捕获以 button 或 anchor 形式渲染的文件链接。

## 0.1.0 - 2026-08-14

### 新功能

- Host 为 DeepSeek Harness Web 提供文件预览和说明文件发现
- Client 把 `read` / `write` / `edit` 做成可点文件行，并打开右侧面板（只读视图 / CodeMirror diff）
- 界面文案跟随 DSH 设置里的语言
